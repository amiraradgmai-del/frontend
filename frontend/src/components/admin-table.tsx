import { Card, CardContent } from "@/components/ui/card";
export function AdminTable({title,subtitle,headers,children}:{title:string;subtitle:string;headers:string[];children:React.ReactNode}){return <div className="space-y-6"><header><p className="text-sm font-medium text-primary">مرکز مدیریت</p><h1 className="mt-1 text-3xl font-black">{title}</h1><p className="mt-2 text-muted-foreground">{subtitle}</p></header><Card className="border-white/70 bg-white/85"><CardContent className="overflow-x-auto p-0"><table className="w-full min-w-[760px] text-right text-sm"><thead className="bg-slate-50 text-muted-foreground"><tr>{headers.map(h=><th key={h} className="p-4">{h}</th>)}</tr></thead><tbody>{children}</tbody></table></CardContent></Card></div>}
